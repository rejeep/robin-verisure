# Robin Verisure temperature check

## Installation

First install Ruby, for example via https://rvm.io/ (the system Ruby
might work as well).

Install the gems:

```ruby
$ bundle
```

## Usage

```ruby
$ bundle exec ruby robin.rb ACCOUNT_ID USERNAME PASSWORD
```
